fields:
    title:
        label: Title
        span: auto
        type: text
    cover:
        label: 'Cover image'
        mode: image
        fileTypes: 'jpg,jpeg,png,gif,svg'
        useCaption: true
        thumbOptions:
            mode: crop
            extension: auto
        span: auto
        type: fileupload
    slug:
        label: Slug
        span: auto
        defaultFrom: title
        preset:
            field: title
            type: slug
        type: text
